server:
  port: 8081
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/repositoryManager?characterEncoding=utf8&serverTimezone=UTC
    username: root
    password: root


# log日志配置
logging:
  level:
    com.miaohy.demo: debug
mybatis:
  mapper-locations: classpath:mapping/*.xml